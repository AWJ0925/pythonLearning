## Tuple(元组)

### 1.创建元组

**注**：

（1）`列表`可以一次性存储多个数据（元素），但列表中的数据是允许修改的；而`元组`中数据是不可修改的

（2）当**元组**的数据只有一个，要在后面加`逗号`，否则不是元组类型的数据

（3）tuple 所谓的“不变”是说，tuple 的每个元素，指向永远不变

### 2.**操作**



### 3. **运算符**

注：这里运算后会生成一个**新元组**。

### 4. **内置函数**

